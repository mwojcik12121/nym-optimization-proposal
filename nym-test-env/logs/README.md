The current run's logs are generated here as
`nodeXX_scenarioN_YYYYMMDD_HHMMSS.log`, using the Docker host's local time.
`run.sh` removes older generated `.log` files before starting a new run.
Application ANSI control sequences are removed from the saved files.
